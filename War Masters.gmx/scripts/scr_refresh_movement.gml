if keyboard_check_released(keyLeft) or keyboard_check_released(keyRight) or keyboard_check_released(keyUp) or keyboard_check_released(keyDown)
{
    if keyboard_check(keyRun)
    {
    accion = "idle";
    }
}
